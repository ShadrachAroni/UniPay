{
  "name": "@unipay/shared",
  "version": "1.0.0",
  "private": true,
  "description": "UniPay shared domain contracts, adapter interfaces, and API types",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
